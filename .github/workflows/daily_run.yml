name: Daily Landslide Prediction

# Automatically runs every day at 03:00 UTC
on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  run-prediction:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true # Essential for large files like .joblib and .gpkg

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Execute Prediction Pipeline
        env:
          EE_PRIVATE_KEY: ${{ secrets.EE_PRIVATE_KEY }}
        run: |
          python daily_prediction.py

      - name: Commit and Push latest maps
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add daily_maps/
          git commit -m "Automated prediction update for tomorrow" || echo "No changes to commit"
          git pull origin main --rebase
          git push
